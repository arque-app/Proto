# Session log — 2026-09-04

*Auto-captured by `~/.claude/hooks/log-session.py`. One entry per assistant turn. Roll up into `CONTEXT.md` at session end.*

## 02:49:43 — Stop (session `6a8a791f`)

**Said:**

Main is clean and pushed. Everything else is on `polish/design-and-node-standard` — one commit, nothing to deploy until you've looked.

**The one design call I made without you** — I'd proposed `page` *expects* `route` and `decision` *expects* `condition`. Building it, that was wrong: it made every quick sketch nag. So `expects` now holds only keys a node genuinely can't function without — `api` → method + path/url, `flow` → doc. Everything else is a suggestion chip in the panel. `Login = page` is a complete, correct node. Push back if you disagree; it's one file.

**The standard** (`src/fml/nodeTypes.ts`, single source of truth): `page` · `api` · `decision` · `event` · `flow`. Off-standard types still draw — warn in loose, error in strict. `api` keys are execution-ready now (`header.<Name>`, `capture.<var>` — the kv lexer takes `.` for exactly this), so what gets written today runs later without a rewrite.

**Design** — cards got a type accent rail, a real glyph per type, selection that glows in the type's own colour, a stacked-card silhouette on `flow` nodes so a portal looks like a portal, dashed borders on undeclared nodes, and `image:` thumbnails on pages. Sidebar collapses on ⌘\ and now selects both ways with the canvas. `fitView` finally accounts for the toolbar and panels, so nodes stop hiding under them.

**Holes I found and closed:** edge notes were the last read-only corner of the write-back path — now editable, and fixing it surfaced a real bug where a note line like `blocked:` could be misread as a group header. Portal drill-down works (double-click a `flow` node). Reset-to-sample exists. `README.md` was literally one line; `CLAUDE.md` still described the tech-tree builder from July — both rewritten. Sample and all examples rewritten to parse clean under strict.

134 tests green, typecheck and build clean.

**One thing I couldn't do:** the Chrome extension isn't connected, so I never saw this render. Every visual claim above is from the code, not a scre...[truncated]

---
## 11:11:43 — Stop (session `6a8a791f`)

**Said:**

Reviewed, fixed what the review turned up, and it's live.

**What the review caught** (all fixed before deploy):
- **Tall cards overlapped their neighbours** — dagre was laying out every node as 190×72, but an `api` card with 5 keys is ~115px. Now `layout.ts` estimates each node's height from its data, so ranks get real spacing. `authLogin` and `Denied` no longer collide.
- **Every node click re-animated the viewport** — the auto-fit effect depended on the `padding` object, which is rebuilt whenever the property panel opens. Padding now lives in a ref; fit only fires on an actual graph change.
- **Edges were nearly invisible** — `#565656` on `#1b1b1b`. Lifted to `#6f6f6f` at 1.75px.
- **`toReactFlow.ts` had stray NUL bytes** in the pairKey separator (pre-existing from the first commit) — made the file diff as binary. Stripped; it reviews as text now.

**Verified live in the browser:** the five types render with their accent rail + glyph + tag, the property panel edits write straight back into the source (changed a method to PUT, confirmed it landed in the file), and double-clicking the `Checkout` flow node drills into the `checkout` doc.

**Shipped:** merged to `main` (`8111524`), deployed to **protoarch.web.app**, branch deleted, lore + memory updated, `main` pushed.

Still your calls, not done: `public/index.html` is dead Firebase boilerplate, `#` in a value still gets eaten by the comment lexer, no breadcrumb on portal drill-down yet. And — the Figma PNG of the Lexend wordmark whenever you've got it.

---
## 11:18:17 — Stop (session `6a8a791f`)

**Said:**

Fixed and redeployed.

**Cause:** `PayFailed -retry> Cart` is a *back edge* — its target sits three ranks above it. It was leaving PayFailed's bottom and entering Cart's top, so the only path was straight up through `payApi` and behind `Cart`.

**Fix:** `useFmlChart` now checks each edge against the laid-out positions. If the target is in an earlier rank, it re-routes the edge onto one side (right when the source is right of the target, left otherwise) and `FlowEdge` draws it as a wide arc that scales with how far the edge has to travel — the same treatment reciprocal/parallel edges already get. Forward edges and parallel edges are untouched.

Verified on the checkout doc: the `retry` edge now arcs cleanly up the right side into Cart, clear of every node body.

Live on **protoarch.web.app**, `main` at `83ed2a6`. Tests still 134 green.

---
## 15:18:57 — Stop (session `6a8a791f`)

**Said:**

Pushed — `main` at `70e36bc`.

**Git state going in:** everything substantive was already committed and pushed (`83ed2a6`). The only outstanding change was the auto-updated session log, now folded into this commit.

**HOW-TO.md — what I added** (the multi-file/multi-flow parts were only mentioned in passing before):

- **`@meta`** — its own section now: `title:` and `base:` (the API root every `api` node's `path:` resolves against; `url:` overrides it).
- **`@doc <name>`** — multiple flows in one file, the implicit `main` doc, doc-local node ids, and the rule that **arrows can't cross docs**.
- **Portals** — a `flow` node's `doc:` key names the `@doc`/`@fof` it stands for; double-click to drill in.
- **`@fof <path> as <name>`** — split flows across files: no extension, `as <name>` required, nesting, circular-import handling, multi-select drop in the viewer.
- File-shape table, error table, and hand-off checklist all extended to match.
- Fixed a stale comment in `examples/multi.fml` that contradicted its own portal usage.

Both `multi.fml` and the `fof/` set still parse clean (`ok: true`).

**For your other Claude:** point it at `Proto/HOW-TO.md` — that's the self-contained spec. `examples/` (esp. `multi.fml`, `fof/app.fml`, `shop.fml`) are working references, and `npm run demo -- <file>` is the check to run when it can't see the canvas.

---
## 15:21:40 — Stop (session `6a8a791f`)

**Said:**

Real talk first: **"FML" already means something very loud on the internet.** Say "check out FML" in a demo or a tweet and half the room hears "f*** my life." SEO's a mess, and every casual mention carries the joke. That's not a dealbreaker if you go in eyes open — some brands lean into edgy — but it's the kind of thing you'd want flagged before the domain's bought.

Second, smaller: FML is *already* the language name (Flowchart Markup Language). Product = language name can work (see Markdown), but then "the FML spec" and "open FML" mean different things depending on context.

If you want the vibe without the baggage, there's room in that family — something where `.fml` files still make sense but the product has its own name. Or keep protoArch as the product and let FML just be the format.

Two questions:
1. Set on literally "FML", or open to a nearby name?
2. Is this a heads-up, or do you want me to scope the actual rename now? It's touching-a-lot-of-strings work — repo, Firebase project `protoarch`, the `protoarch.web.app` domain, `WS_KEY`/localStorage keys, docs, the wordmark you're making in Figma — but it's mechanical. I'd want a plan before I start.

---
## 15:26:04 — Stop (session `6a8a791f`)

**Said:**

Nah — if the double meaning is the *point*, that flips it. "You open your codebase, go FML, then draw it" is a genuinely good hook. Memorable, has a point of view, devs get it in half a second. That beats a safe name nobody repeats. I was pushing back on it being an *accidental* collision; a deliberate wink is a different thing.

Couple things still worth keeping in view, not as objections:

- **In a suit-and-tie room** (if this ever gets near a Stablish-style pitch), you just call it "Flowchart Markup Language" with a straight face and the wink is for everyone else. Costs nothing, but decide now that you're fine doing that.
- **Language name vs product name.** Right now FML = the format. If the app is also just "FML," it's usually fine but occasionally muddy — "the FML spec" vs "open it in FML." Cheap fix: app gets a hair of its own identity — `FML` the format, something like `fml.dev` / "FML Studio" for the viewer. Or fully embrace the collapse like Markdown did. Your call.
- **Grab the domain before committing.** Check `fml.dev` / `fml.app` / `getfml.*` — the name's only as good as the URL you can actually get.

If you're set, I'll scope the rename properly before touching anything — repo name, the `protoarch` Firebase project + `protoarch.web.app`, the localStorage keys, all the doc/README strings, the wordmark. Want me to write that plan up?

---
## 15:29:01 — Stop (session `6a8a791f`)

**Said:**

That works, and it's not a compromise — it's the normal pattern. **Format names stay generic and open; product names wrap them.** Markdown (format) vs Bear/Obsidian (products). GraphQL (spec) vs Apollo (company). SQL vs Postgres. You *want* that separation: if FML catches on and someone else writes an FML renderer someday, protoArch is still protoArch. If you'd made the company "FML," you'd be competing with your own format for the name.

So:
- **FML** — the language. What devs and agents write. "draw the FML for that flow," `.fml` files, the parser, `HOW-TO.md`, the spec.
- **protoArch** — the product/company. The web app, `protoarch.web.app`, the wordmark, the thing you pitch.

Good news: **the repo already reflects this.** `src/fml/` is the language, `CLAUDE.md` opens with "protoArch," Firebase project is `protoarch`, README's the product page. Nothing to rename — you're basically already there.

The one gap is that the *relationship* is implicit. For the devs and the other Claude, it helps to state it outright in one place. I'd add a single line near the top of `README.md`, `HOW-TO.md`, and `CLAUDE.md`:

> **protoArch** is the app. **FML** is the language it reads — a plain-text format for app flows. You write `.fml`; protoArch draws it.

Want me to drop that in? One surgical line each, no other changes.

---
## 15:31:39 — Stop (session `6a8a791f`)

**Said:**

Done — `origin` now points at `https://github.com/arque-app/ProtoArch.git`, fetch verified, HEAD `70e36bc`. GitHub redirects the old URL anyway, but the remote's clean now.

Two things left whenever you want them (parking the naming talk):
- Local folder's still `…/arque/Proto`. Your call to `mv` it — better done between sessions so I don't lose my working dir mid-task.
- `CLAUDE.md` and a couple of `lore/` files still say `arque-app/Proto`. Redirects cover it, so no rush — I'll fix those in the same pass whenever we settle the FML/protoArch wording.

---
## 16:02:45 — Stop (session `815846d0`)

**Said:**

Pushed. For the Firebase deploy, run this yourself:

```
cd ~/projects/arque/Proto && npm run deploy
```

Here's what landed:

**About tab** — click any node → second tab appears. Shows `note` as a readable paragraph, "receives from" list with edge labels, "leads to" list with edge labels. If no note, prompts you to add one.

**Walkthrough section** — bottom of the Sidebar, below the layers. BFS trace of the full flow as a step list: `source → target` with the edge label underneath. Loops are shown with a `↩ loop` marker instead of being cut silently.

Try it with `sync.fml` — you should immediately see the two-sources-of-truth problem laid out as a readable chain instead of a tangled canvas.

---
## 16:05:37 — Stop (session `815846d0`)

**Said:**

Nice. Graph relaxation is a smart addition — force-directed layout would let the canvas breathe instead of being locked into dagre's strict top-down rows. Nodes that are tightly connected pull closer, isolated ones push apart. Makes cycles (like the resync loop) way less tangled visually.

Let them run it. Once it's deployed, drop the stash FML files in and let's see how the sync flow looks with the relaxed layout — that'll tell us faster than anything whether the diagram is actually readable now.

---
## 16:07:09 — Stop (session `6a8a791f`)

**Said:**

**Deploy's done.** Live on protoarch.web.app.

Turned out your other Claude (Sonnet 4.6) had already pushed straight to `main` as `a992180` — so I reviewed the diff rather than pulling loose changes:

- **PropertyPanel** — Props/About tab switcher. "About" reads the node's `note:` as prose and lists what flows in/out with labels, instead of making you parse key-value rows.
- **Sidebar** — a "Walkthrough" section that BFS-traces the active doc into a plain-text step list, marks loops with ↩.

Additive, isolated to two components plus one prop wire-through, style matches ours. typecheck + 134 tests + build green, and I checked both in the browser — they work. Deployed, and paired a lore commit (`6991d4f`) so the push matches the deploy.

Small nits I logged but didn't block on: the walkthrough's `isBack` marks *any* already-seen node as a "loop" (convergent paths can get a false ↩ depending on trace order), and disconnected cycle-islands past the first component don't get walked. Cosmetic, fixable later.

---

**On the layout mess — quick honest take before I build anything.**

Look at that screenshot: the problem isn't nodes overlapping (dagre ranks them fine). It's three things:

1. **Bezier edges cutting diagonally across open canvas and grazing nodes** — the `yes` edge sweeping from HasSession down past Login to Home is the poster child.
2. **Straight chains staircase** — HasSession → Login → authLogin → Home should be one column; instead each step drifts right.
3. **Labels float** — `yes` sits alone mid-canvas, nowhere near its arrow's ends.

Here's the thing: **pure "graph relaxation" (springs + repulsion, let it settle) is the wrong tool for a flowchart.** It fights the top-to-bottom reading order that makes a flow legible — force layouts are for undirected blobs, not ranked journeys. It'd make this *worse*. Where relaxation actually helps is a narrow post-pass: nudging within-rank spacing and separating overlaps.

The real wins, in order of impact:

- **Orthog...[truncated]

---
## 16:18:37 — Stop (session `6a8a791f`)

**Said:**

First pass done — committed locally as `96b9018`, **not pushed or deployed** yet, since you wanted to eyeball it first.

**What changed:**
- **Edges are orthogonal now** — right-angle segments with rounded corners instead of beziers cutting diagonally. Used React Flow's own `getSmoothStepPath`, so no new dep and no hand-rolled routing bugs.
- **Chains straightened** — `align: "UL"` in dagre + wider node/rank spacing. Launch → HasSession hold one column instead of staircasing.
- **Back / reciprocal / parallel edges kept their bowed curve** — so a "goes back around" edge still visually reads as a detour, not a straight line. Checked the checkout doc: the `retry` back-edge still arcs cleanly out the right side.

typecheck + 134 tests + build all green.

**What's still not clean** (candidates for pass 2 if you want to keep going):
- The `yes` edge (HasSession → Home) runs down the far-left gutter with its label stranded in open space — long rank-skipping edges still get their own lonely lane.
- Login / authLogin sit in a right-hand column while Home / Denied are left-shifted — the columns don't line up under a single spine.
- One unavoidable crossing where the `200` edge cuts across the `yes` lane.

Your read — is this better enough to push, and do you want me to take another pass at the long-edge lanes and column alignment?

---
