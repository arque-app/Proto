# Session log — 2026-09-04

*Auto-captured by `~/.claude/hooks/log-session.py`. One entry per assistant turn. Roll up into `CONTEXT.md` at session end.*

## 02:49:43 — Stop (session `6a8a791f`)

**Said:**

Main is clean and pushed. Everything else is on `polish/design-and-node-standard` — one commit, nothing to deploy until you've looked.

**The one design call I made without you** — I'd proposed `page` *expects* `route` and `decision` *expects* `condition`. Building it, that was wrong: it made every quick sketch nag. So `expects` now holds only keys a node genuinely can't function without — `api` → method + path/url, `flow` → doc. Everything else is a suggestion chip in the panel. `Login = page` is a complete, correct node. Push back if you disagree; it's one file.

**The standard** (`src/fml/nodeTypes.ts`, single source of truth): `page` · `api` · `decision` · `event` · `flow`. Off-standard types still draw — warn in loose, error in strict. `api` keys are execution-ready now (`header.<Name>`, `capture.<var>` — the kv lexer takes `.` for exactly this), so what gets written today runs later without a rewrite.

**Design** — cards got a type accent rail, a real glyph per type, selection that glows in the type's own colour, a stacked-card silhouette on `flow` nodes so a portal looks like a portal, dashed borders on undeclared nodes, and `image:` thumbnails on pages. Sidebar collapses on ⌘\ and now selects both ways with the canvas. `fitView` finally accounts for the toolbar and panels, so nodes stop hiding under them.

**Holes I found and closed:** edge notes were the last read-only corner of the write-back path — now editable, and fixing it surfaced a real bug where a note line like `blocked:` could be misread as a group header. Portal drill-down works (double-click a `flow` node). Reset-to-sample exists. `README.md` was literally one line; `CLAUDE.md` still described the tech-tree builder from July — both rewritten. Sample and all examples rewritten to parse clean under strict.

134 tests green, typecheck and build clean.

**One thing I couldn't do:** the Chrome extension isn't connected, so I never saw this render. Every visual claim above is from the code, not a scre...[truncated]

---
## 11:11:43 — Stop (session `6a8a791f`)

**Said:**

Reviewed, fixed what the review turned up, and it's live.

**What the review caught** (all fixed before deploy):
- **Tall cards overlapped their neighbours** — dagre was laying out every node as 190×72, but an `api` card with 5 keys is ~115px. Now `layout.ts` estimates each node's height from its data, so ranks get real spacing. `authLogin` and `Denied` no longer collide.
- **Every node click re-animated the viewport** — the auto-fit effect depended on the `padding` object, which is rebuilt whenever the property panel opens. Padding now lives in a ref; fit only fires on an actual graph change.
- **Edges were nearly invisible** — `#565656` on `#1b1b1b`. Lifted to `#6f6f6f` at 1.75px.
- **`toReactFlow.ts` had stray NUL bytes** in the pairKey separator (pre-existing from the first commit) — made the file diff as binary. Stripped; it reviews as text now.

**Verified live in the browser:** the five types render with their accent rail + glyph + tag, the property panel edits write straight back into the source (changed a method to PUT, confirmed it landed in the file), and double-clicking the `Checkout` flow node drills into the `checkout` doc.

**Shipped:** merged to `main` (`8111524`), deployed to **protoarch.web.app**, branch deleted, lore + memory updated, `main` pushed.

Still your calls, not done: `public/index.html` is dead Firebase boilerplate, `#` in a value still gets eaten by the comment lexer, no breadcrumb on portal drill-down yet. And — the Figma PNG of the Lexend wordmark whenever you've got it.

---
## 11:18:17 — Stop (session `6a8a791f`)

**Said:**

Fixed and redeployed.

**Cause:** `PayFailed -retry> Cart` is a *back edge* — its target sits three ranks above it. It was leaving PayFailed's bottom and entering Cart's top, so the only path was straight up through `payApi` and behind `Cart`.

**Fix:** `useFmlChart` now checks each edge against the laid-out positions. If the target is in an earlier rank, it re-routes the edge onto one side (right when the source is right of the target, left otherwise) and `FlowEdge` draws it as a wide arc that scales with how far the edge has to travel — the same treatment reciprocal/parallel edges already get. Forward edges and parallel edges are untouched.

Verified on the checkout doc: the `retry` edge now arcs cleanly up the right side into Cart, clear of every node body.

Live on **protoarch.web.app**, `main` at `83ed2a6`. Tests still 134 green.

---
